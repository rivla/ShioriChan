<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:key="departureTime"
        android:title="出発時間" />
    <Preference
        android:key="arriveTime"
        android:title="到着時間" />

    <CheckBoxPreference
        android:key="JapanOrNeighborFlg"
        android:title="行き先の範囲を広げる"
        android:summary="（入力したジャンル通りの場所が出ない時に試して下さい）"
        android:defaultValue="false"
        android:checked="false"
        />

    <EditTextPreference
        android:key="departurePlace"
        android:title="出発地点変更"
        android:defaultValue=""
        />
    <CheckBoxPreference
        android:key="lunchFlg"
        android:title="食事の場所を提案する"
        android:defaultValue="true"
        />
    <CheckBoxPreference
        android:key="randomFlg"
        android:title="一つ目の観光地をランダムで選択する"
        android:defaultValue="false"
        />
</PreferenceScreen>